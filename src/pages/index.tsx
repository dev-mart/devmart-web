import type {NextPage} from "next";
import {LandingPage} from "@/components/landing-page/landing-page";

const Home: NextPage = () => {
    return (
        <LandingPage />
    );
}

export default Home;